# port on which prometheus server runs
PROMETHEUS_PORT = 8000

# URL to access the server
PROMETHEUS_URL = f"http://localhost:{PROMETHEUS_PORT}"

# interval to collect metrics
COLLECTION_INTERVAL = 5

# Database Configuration
DATABASE_URL = "postgresql://postgres:Dak4prescott@localhost/predictive_maintenance"